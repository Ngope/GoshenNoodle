import Hero from '../components/Hero';
import Contact from '../components/Contact';

export const metadata = {
  title: "Contact — Goshen Noodles",
};

export default function ContactPage() {
  return (
    <main>
      <Hero />
      <Contact />
    </main>
  );
}
