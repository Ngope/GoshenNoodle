import Hero from '../components/Hero';
import Menu from '../components/Menu';

export const metadata = {
  title: "Menu — Goshen Noodles",
};

export default function MenuPage() {
  return (
    <main>
      <Hero />
      <Menu />
    </main>
  );
}
